[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["app/tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 120
exclude = ["venv"]

[tool.ruff.lint]
# Default rule set (pyflakes + core pycodestyle errors)
select = ["E4", "E7", "E9", "F"]
